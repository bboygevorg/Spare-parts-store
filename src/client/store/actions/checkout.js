import { createActions } from 'redux-actions'

export const checkoutActions = createActions({
    SET_STEP: null,
    SET_SHIPMENT_TYPE: null,
    SET_DELIVERY_ADDRESS_ID: null,
    SET_BILLING_ADDRESS_ID: null
})